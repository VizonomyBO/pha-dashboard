import { IconLayerData } from '../IconLayerData';

describe('Testing IconLayerData function', () => {
  test('IconLayerData should be defined', () => {
    expect(IconLayerData).toBeDefined();
  });
});
